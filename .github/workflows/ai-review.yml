# 檔案名稱：.github/workflows/ai-review.yml

name: AI Code Review

# 觸發條件：當有新的或更新的 Pull Request 針對 main 分支時
on:
  pull_request:
    branches: [ main ]
    types: [opened, synchronize]

# 工作流程權限
permissions:
  pull-requests: write
  issues: read

jobs:
  ai-review-job:
    runs-on: ubuntu-latest

    steps:
      # 步驟 1：檢出專案程式碼
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 步驟 2：獲取本次 Pull Request 的程式碼變更內容 (已修正)
      - name: Get code diff
        id: get-diff
        run: |
          # 【修正處】
          # 我們直接使用 GitHub 提供的 commit SHA 來計算差異，這是最穩定、最可靠的方法。
          # github.event.pull_request.base.sha 是目標分支 的最新 commit
          # github.event.pull_request.head.sha 是來源分支 的最新 commit
          DIFF=$(git diff --unified=0 ${{ github.event.pull_request.base.sha }} ${{ github.event.pull_request.head.sha }})
          
          echo "diff<<EOF" >> $GITHUB_OUTPUT
          echo "$DIFF" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # 步驟 3：呼叫 GLM API 進行程式碼審查
      - name: Call GLM API for review
        id: call-ai
        # 【新增】如果沒有程式碼變更，就跳過這個步驟，節省資源
        if: steps.get-diff.outputs.diff != ''
        run: |
          PROMPT=$(cat <<EOF
          你是一位資深的軟體工程師，請審查以下程式碼變更。
          請從以下幾點提供具體回饋：
          1.  **潛在錯誤**：邏輯錯誤、邊界問題等。
          2.  **程式碼品質**：可讀性、可維護性、命名規範。
          3.  **效能建議**：是否有更高效的寫法。
          4.  **安全性**：是否有潛在的安全漏洞。

          請使用 Markdown 格式回覆，並針對問題點給出程式碼片段作為說明。

          --- 程式碼變更內容 ---
          ${{ steps.get-diff.outputs.diff }}
          --- 程式碼變更結束 ---
          EOF
          )

          RESPONSE=$(curl -s -X POST "https://open.bigmodel.cn/api/paas/v4/chat/completions" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.ZAI_API_KEY }}" \
            -d "{
              \"model\": \"glm-4\",
              \"messages\": [{\"role\": \"user\", \"content\": \"$PROMPT\"}],
              \"temperature\": 0.3
            }")

          REVIEW_TEXT=$(echo "$RESPONSE" | jq -r '.choices[0].message.content')
          
          echo "review<<EOF" >> $GITHUB_OUTPUT
          echo "$REVIEW_TEXT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # 步驟 4：將審查結果發布到 Pull Request
      - name: Post or update review comment
        uses: actions/github-script@v7
        # 【新增】如果 AI 沒有產出審查內容，也跳過這個步驟
        if: steps.call-ai.outputs.review != ''
        with:
          script: |
            const reviewOutput = `${{ steps.call-ai.outputs.review }}`;
            const botIdentifier = '### 🤖 GLM AI 程式碼審查報告';

            // 獲取此 PR 的所有評論
            const comments = await github.rest.issues.listComments({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
            });

            // 尋找機器人上次發布的評論
            const existingComment = comments.data.find(comment => 
              comment.user.type === 'Bot' && comment.body.includes(botIdentifier)
            );

            const newBody = `${botIdentifier}\n\n${reviewOutput}`;

            if (existingComment) {
              await github.rest.issues.updateComment({
                comment_id: existingComment.id,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: newBody
              });
              console.log(`已更新評論 #${existingComment.id}`);
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: newBody
              });
              console.log("已創建新評論");
            }
